# 2026-09-10

## Narra 前端清理：保 UI、删代码

**背景**：`D:\Narra\frontend` 原为 Next.js 15 + TypeScript 项目（原名 openmaic，从 OpenMAIC 开源项目引入，commit `d65618d`）。用户计划用 Vue3 + Vite 重写前端，要求保留 UI 设计、删除其余代码。

**执行结果**：
- 保留：`assets/`（24 文件）、`public/`（96 文件）、`src/styles/globals.css`（578 行，从 `app/globals.css` 移出）、`.gitignore`、`LICENSE`
- 删除：`app/`、`components/`、`lib/`、`packages/`、`render-service/`、`skills/`、`eval/`、`scripts/`、`e2e/`、`configs/`、`types/`、`community/`、`.github/`、`.codegraph/`、`.idea/`，以及全部根级配置文件与文档
- 额外备份：`D:\Narra\_ui_backup\`（121 文件，含 globals.css + assets + public + 图标），用户确认后可删

**产出**：`frontend/docs/UI-还原文档.md`（1200 行）— 含设计 token、依赖映射表、资源清单、**全部 5 个页面的完整 UI 结构**（JSX 骨架 + 真实 Tailwind 类名 + 动画参数 + 中英文文案 + 图标清单）、重写检查清单。

**关键结论**：
1. 设计 token 框架无关（OKLCH + CSS 变量），Vue 端装 `tailwindcss@4` + `@tailwindcss/vite` + `tw-animate-css` 可直接复用。
2. `globals.css` 有 4 处失效引用**必须删除**才能启动：第 3 行 `@import 'shadcn/tailwind.css'`、第 5 行 `@import '../components/workbench/chat/workbench-chat.css'`、第 9–12 行 4 条 `@source '../node_modules/...'`、第 13–16 行 `@source "./slide-renderer-demo/..."`。保留开头仅 `@import 'tailwindcss'` + `@import 'tw-animate-css'`。
3. 品牌主色 `--primary: #722ed1`（亮）/ `#8b47ea`（暗）。工作区另有独立 token（`--ws-accent #7c3aed`）。
4. 暗色模式用 `.dark` class 变体（非 media query），三态 light/dark/system。
5. 源码从未丢失：远程 `git@github.com:qianqiang-del/Narra.git` 的 HEAD 即 `d65618d`，可用 `git archive d65618d frontend/...` 随时取回。分析用副本 `_src_ref/` 已删。

**页面要点**：
- 首页：Composer 统一输入卡（GreetingBar + AgentBar + GenerationToolbar）+ 最近学习折叠区。
- 课堂页：三栏 = 左场景栏(220/170~400) + 中舞台 + 右聊天(340/240~560)；Header 硬编码 80px、Roundtable 192px，舞台高度 `calc(100% - 80px - 192px)`。
- 生成预览页：4 个互斥分支（加载/空态/大纲审阅/主流程），6 种 StepVisualizer 动画。
- 工作区：三栏可折叠（折叠用 `display:none` 不卸载），视觉层在 `workspace-shell.css`（`--ws-*` token），Pro↔classic 用 View Transition。

## Vue3 + Vite 脚手架（已搭建完成）

**技术栈**：Vue 3.5 + Vite 8 + TS 6 + Tailwind v4（`@tailwindcss/vite`）+ Vue Router 4 + Pinia 4 + vue-i18n 11 + lucide-vue-next + motion-v + reka-ui + vue-sonner + `@fontsource-variable/inter`。

**关键配置**：
- `vite.config.ts`：`@` → `./src` 别名；`/api` 代理到 `http://localhost:8080`（Go 后端）；端口 5173。
- `tsconfig.app.json`：**TS 6 已废弃 `baseUrl`**，`paths` 直接写 `{"@/*": ["./src/*"]}`，不能配 `baseUrl`（否则报 TS5101）。
- `src/styles/globals.css`：已删 4 处失效引用。**注意：注释里也不能出现 `{js,cjs}` 这类花括号文本**，Tailwind v4 会当语法解析并报 `CssSyntaxError: Invalid declaration`。
- `index.html`：含首帧内联脚本应用主题，防暗色闪白。
- `src/composables/useTheme.ts`：三态主题，localStorage 键 `narra-theme`。
- `src/i18n/index.ts`：i18n 实例 + `setLocale()`，localStorage 键 `narra-locale`；仅有 zh-CN / en-US 两套。

**页面骨架**（均为占位，待按文档填充）：`HomeView`、`WorkspaceView`、`ClassroomView`、`GenerationPreviewView`、`WorkbenchNewView`（自动跳 /workspace）、`NotFoundView`。

**验证结果**：`npm run build` 通过（71 模块，CSS 90.5KB）；`npm run dev` HTTP 200；产物中确认 `--primary:#722ed1`、`100dvh`、`scrollbar-hide`、dark 变体均已生成。

**注意**：Vite 8 用 rolldown 构建。`pnpm create vite` 会因 pnpm dlx 缓存报 `ERR_PNPM_CLI_DLX_READ_MANIFEST`，改用 `npm create vite` 即可。

**待办**：按 `docs/UI-还原文档.md` 从首页（§5）开始逐步实现界面。

## 首页全部补完（§5.0–§5.10 已完成）

**新增组件**（`src/components/home/`）：
- `TopPillToolbar.vue`（§5.1）语言切换 + 主题三态 + 设置齿轮
- `ProBadge.vue`（§5.3）"Pro" 药丸
- `GreetingBar.vue`（§5.4）头像 + 昵称编辑 + 头像选择器（内置 7 个 + 自传压 128×128）+ 简介；资料存 localStorage 键 `narra-profile`
- `AgentBar.vue`（§5.5）教师行 + 预设/自动 Tab + 5 个预设角色 + 自动模式脉冲圈
- `AgentVoicePill.vue`（§5.5）音色 pill，带搜索 Popover
- `GenerationToolbar.vue`（§5.6）模型选择器（左服务商/右模型双列 640px）+ 课程材料（拖拽上传）+ 联网搜索 + 媒体生成
- `RecentSection.vue`（§5.7）折叠区：分隔线触发条 + 搜索 + 导入 + 新建文件夹 + 面包屑 + 网格
- `ClassroomCard.vue`（§5.8）16:9 缩略图 + 模式徽章 + hover 操作 + 删除确认遮罩 + 双击重命名
- `FolderCard.vue`（§5.9）扇形封面堆叠 + 拖拽放置 + 两种删除确认（仅文件夹 / 连课程）
- `NewFolderDialog.vue`（§5.10）

**新增数据/状态**：
- `src/data/agents.ts`（TEACHER + PRESET_ROLES）
- `src/data/voices.ts`（VOICE_GROUPS + `voiceName()`）
- `src/data/providers.ts`（10 个模型服务商，logo 复用 `public/logos/`）
- `src/stores/library.ts`（Pinia，课程/文件夹 CRUD + `formatRelativeDate()`，localStorage 键 `narra-library`，首次含 6 门课程 + 2 个文件夹种子数据）

**`App.vue` 加了 `<Toaster>`（vue-sonner）** —— 之前缺这个，toast 不会渲染。同时引入 `vue-sonner/style.css`。

**踩坑**：
1. reka-ui **没有 `Checkbox` 导出**，是 `CheckboxRoot` + `CheckboxIndicator`（配合 `<CheckboxIndicator><Check/></CheckboxIndicator>`）。写 AgentBar 时按 shadcn 习惯写成 `Checkbox` 直接报 TS2305。
2. `UiTooltip` 的默认 slot 即 trigger，**没有具名 content slot**；想给 tooltip 内容加按钮必须另想办法（改成了把复制按钮放在卡片信息行里）。
3. 父组件监听多参事件时不要写 `@evt="fn($event, $event)"`，要写 `@evt="(a, b) => fn(a, b)"`。

**验证**：`npm run build` ✓（2421 模块，HomeView chunk 154KB，CSS 119.9KB）；所有新模块 dev server 均返回 200。

**明天继续**：
- 首页视觉验收 —— 想截图对比，`agent-browser` 已装上（全局 `D:/node/node_global`），但 **Chromium 还没下完**（`agent-browser install` 中断）。明天先跑该命令，再 `agent-browser open http://localhost:5173/`。注意 `agent-browser` 不在默认 PATH 里，需 `export PATH="D:/node/node_global:$PATH"`。
- 端口：5173 上有个旧 dev server 在跑（HTTP 200），新起的落到 5174。明天先清掉旧进程再用 5173。
- 之后按文档推进：课堂页 §6 → 生成预览页 §7 → Pro 工作区 §8。

