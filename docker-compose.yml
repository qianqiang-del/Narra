services:
  embedding:
    build:
      context: ./embedding-service
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - bge-model-cache:/models
    restart: unless-stopped

volumes:
  bge-model-cache:
